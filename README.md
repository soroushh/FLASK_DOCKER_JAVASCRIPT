# FLASK_DOCKER_JAVASCRIPT
A project trying to learn javascript libraries.

# Running the test

For running the tests, run the following command in the root of the rpoject:
docker-compose run web pytest usr/src/app/tests